language: rust
sudo: false
dist: trusty
matrix:
    fast_finish: true
    include:
        - rust: stable

cache:
    apt: true
    directories:
        - target/debug/deps
        - target/debug/build
